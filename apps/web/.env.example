# Copy to .env.local for local development. Never commit .env.local.
#
# The public base URL of the API. Locally that is the uvicorn process; in
# production it is the Railway service URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
